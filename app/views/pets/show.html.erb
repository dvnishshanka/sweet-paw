<div class="container-show">
  <div class="flex-wrap">
    <div class="img-fluid show-image animate__animated animate__rollIn animate__delay-.5s">
      <% if @pet.photo.key%>
                  <%= cl_image_tag @pet.photo.key, :height=>500, :width=>500, :fill=>"fill" %>
                <% else %>
                  <div class="card-product-show">
                    <div class="card-image-show">
                      <div class="card-product img">
                        <% if @pet.species == "dog" %>
                        <%= image_tag ("dog.png")%>
                        <% elsif @pet.species == "monkey" %>
                        <%= image_tag("monkey.png")%>
                        <% elsif @pet.species == "cat" %>
                        <%= image_tag("cat.png")%>
                        <% elsif @pet.species == "piglet" %>
                        <%= image_tag("piglet.png")%>
                        <% elsif @pet.species == "rabbit" %>
                        <%= image_tag("rabbit.png")%>
                        <% end %>
                      </div>
                    </div>

                  </div>

          <% end %>
    </div>
    <div>
      <h3 class="animate__animated animate__jackInTheBox animate__delay-.8s show-image">Allow me to introduce you to <%= @pet.name %> </h3>
    </div>
  </div>

</div>

<div class="container-show">
  <div class="ul-container-show">
    <div class="show-info-pet animate__animated animate__jackInTheBox animate__delay-.8s">
        <p>Address: <%= @pet.address %></p>
        <p>Species: <%= @pet.species.capitalize %> </p>
        <p>Age: <%= @pet.age %></p>
        <p>A little information about <%= @pet.name %> </p>
        <p><%= @pet.description %>🌈</p>
    </div>
  </div>
</div>

<div class="container" data-controller="insert-in-list">
<h2><%= pluralize @pet.reviews.size, "review" %></h2>


  <div data-insert-in-list-target="items">
  <% if @pet.reviews.blank? %>
    Be the first to leave a review for <%= @pet.name %>
  <% else %>
    <% @pet.reviews.each do |review| %>
    <%= render "reviews/review", review: review %>
    <% end %>
  <% end %>
  </div>

<%= render 'reviews/form', pet: @pet, review: @review %>
</div>





<ul class="show-btn">
      <li><%= link_to "Back to all Pets", pets_path, class: "btn-return" %></li>
      <li><%= link_to "Book this Pet", new_pet_booking_path(@pet), class: "btn-book-pet" %></li>
</ul>
